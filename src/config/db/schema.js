export default {
    tokens: `++id, 
             userId,  
             chainId, 
             contract, 
             name, 
             symbol,
             decimals
            `,

    balances: `id, 
               token, 
               wallet, 
               balance,
               balanceDecimal,
               balanceUSD,
               updatedAt
            `
}